#include "shell.h"
/**
*main - environment function
*@ac: parameter one
*@av: parameter two
*Return: always 0
*/
int main(int ac, char *av)
char *environmen_variable
while (environment_variable[m] != null)
{
write("%d: %s\n", m, environment_variable[m]);
m++;
}
return (0);
}
