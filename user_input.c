#include "shell.h"
/**
*main - takes user instructions function
*@instruction: parameter one
*@size_t: parameter two
*@size: parameter three
*Return: always 0
*/
void presenting_instructions(char *instruction, size_t, size);
if (fgets(instructions, size, 0) == null)
{
if (feof(0))
{
evody_print("\n");
exit(exit_success);
}
else
{
evody_print("error in presenting instructions\n");
exit(exit_failure);
}
}
